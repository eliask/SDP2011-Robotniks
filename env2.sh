#! /bin/bash
# This is used for running the system on machines that have their
# native versions of the Python packages installed. The supplied
# packages only run on 32-bit x64 machines.

. env.sh
PYTHONPATH=
export MPLAYER_CAPTURE=0
